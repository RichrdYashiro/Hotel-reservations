const initialState = {
  rooms: [],
  loading: false,
  error: null,
};

export const roomsReducer = (state = initialState, action) => {
    switch (action.type) {
    default:
			return state;
    }
}