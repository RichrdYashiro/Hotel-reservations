
const initialState = {
  bookings: [],
  loading: false,
  error: null,
};

export const bookingReducer = (state = initialState, action) => {
    switch (action.type) {
    default:
			return state;
    }
}