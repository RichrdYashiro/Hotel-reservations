const initialState = {
  user: null,
  isAuthenticated: false,
  role: null,
};

export const userReducer = (state = initialState, action) => {
    switch (action.type) {
    default:
			return state;
    }
}